<div class="container">
  <div class="league-name">
    <h2><%= @league.name %></h2>
  </div>
  <table class="table">
    <thead>
      <tr>
        <th></th>
        <th>チーム名</th>
      </tr>
    </thead>
    
    <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.short_name %></th>
        <td><%= link_to team.name, team_path(team.id) %></td>
      </tr>
    <% end  %>
    </tbody>
  </table>
  <%= link_to 'リーグ一覧', leagues_path %>
</div>