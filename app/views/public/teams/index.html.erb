<div class="container">
  <table class="table">
    <thead>
      <tr>
        <th>所属リーグ</th>
        <th>チーム名</th>
        <th></th>
      </tr>
    </thead>
    
    <tbody>
      <% @teams.each do |team| %>
      <tr>
        <td><%= team.league.name %></td>
        <td><%= link_to team.name, team_path(team.id) %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>